// import { assertThrowsAsync } from "../deps.ts"

// test
// import { cli } from './cli.ts'


// TODO: mock
Deno.test('it should display error if called with no script', async () => {
  // Arrange
  const args: Array<string> = ['unshell', 'run']
  const env = {}

  // Mock

  // Assert
  // assertThrowsAsync(() => cli({ args, env }))
})
